# Database
# Supabase PostgreSQL Database (Production)
# Get your connection string from Supabase project settings
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-ap-northeast-2.pooler.supabase.com:6543/postgres"

# Direct URL (Required for Prisma migrations)
# Use Direct connection (port 5432) for migrations
# Get from Supabase Dashboard → Settings → Database → Connection string → URI → Mode: Session
DIRECT_URL="postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres"

# 로컬 개발용 (PostgreSQL or SQLite)
# For local development, you can also use:
# DATABASE_URL="postgresql://localhost:5432/alles_inventory"
# Or SQLite: DATABASE_URL="file:./dev.db"

# 클라우드 배포용 (PostgreSQL - Neon - Alternative)
# Neon 데이터베이스 연결 URL
# https://neon.tech 에서 프로젝트 생성 후 연결 URL을 복사하여 입력하세요
# DATABASE_URL="postgresql://[user]:[password]@[host]/[database]?sslmode=require"

# 환율 API (선택 - 한국수출입은행 API)
# API 키 없이도 사용 가능하지만, 공식 API 키가 있으면 더 안정적입니다
# 발급: https://www.koreaexim.go.kr/ir/HPHKIR020M01
KOREAEXIM_API_KEY="sample"

# 프록시 서버 URL (선택 - 한국 정부 API 호출용)
# Vercel 서버리스 환경에서 한국 정부 API 호출 시 SSL/네트워크 문제 해결을 위한 프록시
# Railway에 배포된 프록시 서버 URL을 설정하면 환율 API 및 UNI-PASS API 호출이 프록시를 경유합니다
# 설정하지 않으면 기존 직접 호출 방식을 사용합니다 (폴백)
PROXY_SERVER_URL="https://korean-api-proxy-production.up.railway.app"

# 파일 저장소 설정 (오프라인/NAS 공용)
# 기본값: ./public/uploads
# NAS 마운트 경로 예시: /mnt/nas/alles-inventory/uploads
FILE_STORAGE_ROOT="./public/uploads"

# 브라우저 접근용 파일 URL prefix
# NAS를 리버스 프록시로 공개할 때 예시: https://files.company.local/uploads
FILE_PUBLIC_BASE_URL="/uploads"

# 인증(예정) - 세션 토큰/비밀번호 해시 보안용 시크릿
# 운영에서는 충분히 긴 랜덤 문자열로 변경하세요.
AUTH_SECRET="change-this-to-a-long-random-secret"
